{% if template contains 'product' -%}
    {%- unless product.vendor contains 'CustomEasy'
      or product.vendor contains 'PODLovers'
      or product.tags contains 'customily'
      or product.tags contains 'no-personalizer'
    -%}
      {%- render 'pod-personalizer' -%}
    {%- endunless -%}
  {%- endif %}
  {% schema %}
  {
    "name": "Party",
    "target": "body",
    "settings": []
  }
  {% endschema %}